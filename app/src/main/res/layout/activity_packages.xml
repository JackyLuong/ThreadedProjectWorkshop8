<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/grey_bg"
    tools:context=".Packages.PackagesActivity">

    <TextView
        android:id="@+id/tvPackages"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:layout_marginBottom="10dp"
        android:gravity="center"
        android:letterSpacing=".1"
        android:text="Packages"
        android:textColor="@color/black"
        android:textSize="25dp" />


    <ListView
        android:id="@+id/lvPackages"
        android:layout_width="wrap_content"
        android:layout_height="500dp"
        android:layout_below="@id/tvPackages"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"

        android:divider="@color/light_orange"
        android:dividerHeight="4px" />

    />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnAddPackages"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/lvPackages"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="20dp"
        android:height="60dp"
        android:backgroundTint="@color/dark_orange"
        android:text="Add"></com.google.android.material.button.MaterialButton>











</RelativeLayout>